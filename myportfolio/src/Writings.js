import React from "react";
import styled from "styled-components";

const Writings = () => {
  return <Wrapper>Wriitng</Wrapper>;
};

const Wrapper = styled.div`
  height: 90%;
  width: 80%;
  background-color: white;
`;

export default Writings;
